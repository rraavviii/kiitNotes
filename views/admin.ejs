<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .admin-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 80%;
            max-width: 800px;
        }
        .admin-container h2 {
            margin-bottom: 20px;
        }
        .admin-container table {
            width: 100%;
            border-collapse: collapse;
        }
        .admin-container table, th, td {
            border: 1px solid #ccc;
        }
        .admin-container th, td {
            padding: 10px;
            text-align: left;
        }
        .admin-container th {
            background-color: #f2f2f2;
        }
        .admin-container .actions {
            display: flex;
            justify-content: space-around;
        }
        .admin-container .actions button {
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .admin-container .actions .edit {
            background-color: #5bc0de;
            color: #fff;
        }
        .admin-container .actions .delete {
            background-color: #d9534f;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <h2>Admin Dashboard</h2>
        <p>Logged in as: <%= user.email %></p>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <!-- Example row -->
                <tr>
                    <td>1</td>
                    <td>JohnDoe</td>
                    <td><%= user.email %></td>
                    <td class="actions">
                        <button class="edit">Edit</button>
                        <% if (user.email === 'ravikumar@gmail.com') { %>
                            <a href="/newe"><button class="delete">Delete</button></a>
                        <% } %>
                    </td>
                </tr>
                <!-- Add more rows as needed -->
            </tbody>
        </table>
    </div>
</body>
</html>